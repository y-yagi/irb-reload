# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in irb-reload.gemspec
gemspec

gem "rake"
gem "minitest"
gem "minitest-mock"
gem "logger" # FIXME: This is for `listen` gem dependency. Remove it when `listen` releases a new version.
